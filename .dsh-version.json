{
  "comment": "The DeepSeek Harness version line this plugin pack is built and tested against. Plugins are shipped as alpha until the owner promotes them to stable. The installer pins this exact dsh version when driving the CLI. 'vendoredFrom' records the harness line whose client bundles dsh-rightbar / dsh-rightbar-files / dsh-open-in-app were forked from. dsh-vn-master is the pack's blank master: the bundle layer alone, no browser half.",
  "dsh": "0.1.5-rc.1",
  "vendoredFrom": "0.1.5-rc.1",
  "platforms": [
    "win32",
    "darwin",
    "linux"
  ],
  "comment_platforms": "The plugins are plain JavaScript and platform-neutral; the only OS-specific code is a launcher choosing the right command for the host: dsh-open-in-app's file-browser launcher (explorer.exe / open / xdg-open, WSL-aware) and dsh-terminal's shell resolver (pwsh.exe or powershell.exe / $SHELL or /bin/zsh / $SHELL or /bin/bash). The launchers run on Windows (install.bat, PowerShell 5.1 or 7) and on macOS/Linux (install.sh, PowerShell 7+).",
  "packages": {
    "dsh-vn-master": {
      "version": "0.1.0-alpha.1",
      "rows": [
        "master"
      ],
      "role": "the pack's master and deliberately blank: the bundle layer alone (installed last, so it is the final word per row); no dsh.client, no service, no inject edge, and no core-row disables - those stay with the packages that replace the rows",
      "rowOverrides": []
    },
    "dsh-rightbar": {
      "version": "0.1.0-alpha.1",
      "rows": [
        "rightbar"
      ],
      "disablesRows": [
        "ui-sidebar-right",
        "ui-sidebar-files"
      ],
      "vendors": [
        "@deepseek-ai/dsh-client-ui-sidebar-right"
      ]
    },
    "dsh-rightbar-files": {
      "version": "0.1.0-alpha.1",
      "rows": [
        "rightbar-files"
      ],
      "vendors": [
        "@deepseek-ai/dsh-client-ui-sidebar-files"
      ]
    },
    "dsh-editor": {
      "version": "0.1.0-alpha.8",
      "rows": [
        "editor"
      ],
      "rowOverrides": [],
      "uses": [
        "dsh-modal",
        "@deepseek-ai/dsh-client-ui-theme",
        "@deepseek-ai/dsh-client-ui-sidebar-documentpreview"
      ],
      "shadowsSlots": [
        {
          "slot": "sidebar.right.tab.document",
          "key": "@deepseek-ai/dsh-client-ui-sidebar-documentpreview/markdown",
          "priority": -10,
          "why": "the rendered Markdown body carries the Edit toggle back into the editor (lower priority renders) and undoes what the preview's plain-text scrollport imposes on its contents (white-space:pre and the mono font stack)"
        }
      ]
    },
    "dsh-gittree": {
      "version": "0.1.0-alpha.3",
      "rows": [
        "gittree"
      ],
      "rowOverrides": [],
      "role": "the workspace's COMMIT HISTORY as a page tab type, labelled History in the capsule and the chip while the package, row and address keep the gittree name (guide order 30, after Files and Editor), with the branch and current commit in its file bar; its Node half owns read-only /api/dsh-gittree routes, and the tab reads state with brief=1 so no file list is ever built - no fork, no core row disabled, nothing for sync-vendored.ps1 to track",
      "uses": [
        "dsh-rightbar",
        "git on PATH"
      ]
    },
    "dsh-browser": {
      "version": "0.1.0-alpha.1",
      "rows": [
        "browser"
      ],
      "rowOverrides": [],
      "role": "a BROWSER TAB on the bar - an address bar, back / forward / reload / stop, a connection LED, Copy and Open - whose page is drawn by the browser already showing the GUI (one iframe), so the harness ships no engine and downloads nothing; the page type carries one guide entry at order 40, so Browser sits on the Start page beside Files (10), Editor (20) and History (30). Its only host surface is GET /api/dsh-browser/probe, which fetches an address from the host (Node's own TLS, never system tools) and reports X-Frame-Options / CSP frame-ancestors, so the tab blocks only a REAL framing refusal and annotates every other answer (401, DNS, timeout, non-HTML) as a note - the frame uses the browser's own session, so an anonymous check must not stop it. A cross-origin frame is opaque: in-frame navigation is invisible, so back/forward replay the addresses the tab loaded, and the render surface is one entry in a SURFACES map, so a harness-owned engine (CDP streamed into a canvas) can be added later without touching the toolbar, the tab type or the Start entry",
      "uses": [
        "dsh-rightbar"
      ]
    },
    "dsh-terminal": {
      "version": "0.1.0-alpha.3",
      "rows": [
        "terminal"
      ],
      "rowOverrides": [],
      "role": "a real shell in a bottom dock: a header control (order 30, immediately right of Open In...) toggles a horizontal panel that starts at the left bar's right edge, runs to the full page width and sits UNDER the middle and right columns, which make room for it - those two columns (the frame's FIRST element child is the left bar and is never touched) get height calc(100% - Hpx) while the dock is open and the inline height they had before this plugin ran when it closes, so the left bar keeps its full height and nothing in it moves; the dock's left edge is the frame's resolved gridTemplateColumns first track, so it follows the left bar opening, collapsing and being dragged. Every resize re-fits the emulator (rows/cols from the new box) and scrolls to the end of the output, so the visible line count matches the panel and the newest lines stay in view. xterm.js is vendored (lib/vendor/xterm.js + xterm.css, GENERATED from vendor/) the way the editor vendors CodeMirror, and the Node half owns /api/dsh-terminal/health, /api/dsh-terminal/vendor/* and ONE authenticated WebSocket upgrade (/api/dsh-terminal/pty) that attaches a browser xterm to a real PTY from the HARNESS'S OWN node-pty - resolved, never installed (anchors: process.argv[1], $DSH_HOME/profiles, this package), with a graceful 'no PTY on this host' instead of a broken boot. One PTY per (conversation, slot), kept five minutes after its last socket so a reload reattaches with a scrollback replay, reaped otherwise. No fork, no core row disabled, nothing for sync-vendored.ps1 to track",
      "uses": [
        "@deepseek-ai/dsh-client-ui-conversation",
        "the harness installation's node-pty and ws"
      ],
      "vendors": [
        "@xterm/xterm@5.5.0",
        "@xterm/addon-fit@0.10.0"
      ]
    },
    "dsh-modal": {
      "version": "0.1.0-alpha.1",
      "rows": [
        "modal"
      ],
      "provides": [
        "modals"
      ],
      "rowOverrides": []
    },
    "dsh-themes": {
      "version": "0.1.0-alpha.10",
      "rows": [
        "themes"
      ],
      "rowOverrides": [],
      "uses": [
        "@deepseek-ai/dsh-client-ui-theme",
        "@deepseek-ai/dsh-session-log-export"
      ],
      "role": "the pack's conversation-header package, owning three controls in the header utilities list: the Screenshot control (order -30, one step left of Themes) captures the whole window in the browser - getDisplayMedia on the current tab, one frame into a canvas, PNG out; the app is a fixed-viewport shell, so 100% width and 100% height are exactly the tab's box - and POSTs the bytes to this package's own authenticated host route (/api/dsh-themes/screenshot), which resolves the host's Desktop per request (Windows plain or OneDrive-redirected, XDG on Linux, home as the last resort), validates the PNG signature and size, and writes the file create-exclusively as vn-harness-<timestamp>.png, answering the path; a profile without that row falls back to an ordinary browser download. The Themes control (order -20) drives ui-theme's own preference, and the Session-log download seat (priority -10) shadows the shipped three-dot button with one download icon while the shipped controller keeps doing the export",
      "overrides": [
        "markdown paper: re-declares ui-theme's light declarations on the rendered Markdown root, so the page stays white in the dark theme",
        "markdown chrome: hides the preview header's viewer menu on Markdown tabs (the page has exactly one renderer; the editor's Edit button is the way back to editing)",
        "left top bar: the sidebar branding row becomes the 76px band the middle and right columns open with, its hairline at the same y=76 in both rail states, with the mark, brand name and collapse control on the band's top strip and the core's 8px/12px gap kept under the line; pinned to the sidebar's hashed class names",
        "branding: the left bar brand row drops the shipped mark and wordmark - whatever occupies the single-occupant sidebar.brand.mark / sidebar.brand.name slots, including the layout fallback - and draws a 24px black disc plus the product text VN Harness in their place, in the wide row and in the collapsed rail",
        "branding type: the VN Harness text wears the conversation title's own type - 14px/20px at weight 500, the size of ui-conversation's current crumb - instead of the shipped brand name's 18px/600, which sat in the same 30px strip",
        "branding mark: the drawn disc is replaced by the app icon itself - assets/vn-harness.svg at the pack root, a black circle centred on (12,12) with a 1px transparent margin inside its box - inlined as a data URI so no route or request is needed, and painted at 24px in the left bar (wide row and rail) and 26px in the empty conversation hero, where the shipped whale is also hidden",
        "header ring: the right bar's own collapse/expand toggle in the header corner wears the same .5px round hairline outline as this package's own header icon buttons, keyed on the header's stable data-conversation-header-corner marker rather than a hashed class",
        "session-log download seat: the shipped @deepseek-ai/dsh-session-log-export browser half's three-dot button - whose one menu item was Download session log - is shadowed by registering the SAME occupant id (session-log-download) at priority -10, which is a list slot's own lowest-renders rule, so the seat draws one plain download icon button that starts the export on the first click; nothing shipped is disabled, because that row owns the host half (/api/session.export and the /export command) and publishes the sessionLogDownload controller this button drives, and the seat's preparing/success/error dialog is rendered here from that same store so /export keeps its feedback",
        "capture rule: for the one frame the Screenshot control grabs, html[data-dsh-screenshot] hides this package's three header controls (and any tooltip hanging off them), so the picture is the app rather than the buttons that took it"
      ]
    },
    "dsh-open-in-app": {
      "version": "0.1.0-alpha.1",
      "rows": [
        "native-open-in-app"
      ],
      "disablesRows": [
        "ui-open-in-app"
      ],
      "vendors": [
        "@deepseek-ai/dsh-client-ui-open-in-app"
      ],
      "vendoredPatches": 2
    }
  }
}
